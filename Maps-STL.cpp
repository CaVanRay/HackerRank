#include <iostream>
#include <map>
using namespace std;

int main() {

int rounds, type, grade;
string name;
map<string, int> Students;

cin >> rounds;
    
    while(rounds--){
        cin >> type;
        switch(type){
            case 1: //Add

            break;
            case 2: //Delete

            break;
            case 3: //Search

            break;
        }
    }
    
    return 0;
}
