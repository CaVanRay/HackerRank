#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    
    int n, temp, removeUno, removeFrom, removeTo;
    vector<int> Shrinky;
    cin >> n;
    
    for(int i = 0; i < n; i++){
        cin >> temp;
        Shrinky.push_back(temp);
    }
    cin >> removeUno;
    
    return 0;
}
