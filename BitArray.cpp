/********************************************
Difficulty: Hard
Title: Bit Array
Author: Cavan Ray Theiss

Description: 
Count the number of distinct integers in a 
sequence generated by using the provided
four integers N, S, P, & Q
********************************************/

#include <iostream>
#include <bitset>
#define MULT 2147483648

int main() {
    long long previous = 0, current = 0, totalCount = 0, N, S, P, Q;
    std::cin >> N >> S >> P >> Q;
    std::bitset<MULT>* alreadySeen = new std::bitset<MULT>();
    for(int i = 0; i < N; i++){
        if(i == 0){
            current = (S % MULT);
        }else{
            current = (((previous * P) + Q) % MULT);
        }
        if(alreadySeen->test(current)){
            break;
        }
        alreadySeen->set(current);
        totalDistinct++;
        previous = current;
    }
    std::cout << alreadySeen->count() << std::endl;
    delete alreadySeen;
    return 0;
}
