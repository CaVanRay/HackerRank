#include<iostream>
#include<set>
using namespace std;

int main() {

    int rounds, type, item;
    set<int>S;
    
    cin >> rounds;
    
    while(rounds--){
        
        cin >> type >> item;
        
        switch(type) {
            case 1: //Add
                
                break;
            case 2: //Delete
            
                break;
            case 3: //Search
            
                break;
        }
    }
    
    return 0;
}
